<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title>后台管理系统</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/bootstrap.min.css' />
    <style>
      form {
        text-align: center;
      }
      form table thead tr th{
        text-align: center;
        font-size:20px;
      }
      h1{
          color: #f60;
      }
      .pagination{
         position: absolute;
         left: 50%;
          top:50%;
          transform: translate(-50%,-50%);
      }
      .active{
          background: #f60;
      }  
    </style>
      <script src="/javascripts/jquery.min.js"></script>
      <script src="/javascripts/bootstrap.min.js"></script>
  </head>
  <body>
  <h3 style="margin: 50px auto; text-align: center;color: #f60;">商品列表信息</h3>
    <form>
    <table class="table table-striped  table-bordered table-hover">
      <thead>
        <tr class="text-primary active bg-success">
          <th>订单编号</th>
          <th>下单日期</th>
          <th>商品编号</th>
          <th>数量</th>
          <th>单价</th>
          <th>总金额(元)</th>
          <th>实付金额（元）</th>
          <th>负责人</th>
          <th>负责人email</th>
          <th>编辑</th>
        </tr>
      </thead>
      <tbody>
        <%docs.forEach(function (i) {%>
        <tr>
         <td name="_id"><%=i._id%></td>
         <td name="signAt"><%=i.signAt%></td>
         <td name="brandCode"><%=i.brandCode%></td>
         <td name="number"><%=i.number%></td>
         <td name="price"><%=i.price%></td>
         <td name="shouldPay"><%=i.price*parseInt(i.number)%></td>
         <td name="alreadyPay"><%=i.alreadyPay%></td>
         <td name="person"><%=i.person%></td>
         <td name="email"><%=i.email%></td>
         <td><a class='link' href="/detail?id=<%=i._id%>">查看</a></td>
        </tr>
           <%})%>
      </tbody>
    </table>
    </form>
  <div class="container">
      <div class="row">
          <div class="col-md-8 col-md-offset-2" style="background: red;">
              <nav aria-label="Page navigation">
                  <ul class="pagination aaa">
                      <li>
                          <a href="#" class="prev" aria-label="Previous">
                              <span aria-hidden="true">&laquo;</span>
                          </a>
                      </li>
                      <%for(var i=1;i<=Math.ceil(count/6);i++){%>
                      <li><a  href="/list?page=<%=i%>"><%=i%></a></li>
                      <%}%>
                      <li>
                          <a href="#" class="next" aria-label="Next">
                              <span aria-hidden="true">&raquo;</span>
                          </a>
                      </li>
                  </ul>
              </nav>
          </div>
      </div>
  </div>


  <script>
    $(function(){
       &('.prev').on('click',function () {
           alert(window.location.href)
       })
    });
  </script>
  </body>

</html>
